/// Support contact channels surfaced in `ContactSupportSheet`.
///
/// Centralized so phone numbers / handles / emails are not scattered across
/// widget files. Move to remote config once the support team needs to update
/// these without a new build.
abstract final class SupportContacts {
  static const whatsappPhone = '+996 702 31-36-11';
  static const telegramHandle = '@Duu1at';
  static const supportEmail = 'dbolsunbekuulu@gmail.com';
  static const callPhone = '+996 702 31-36-11';
}
