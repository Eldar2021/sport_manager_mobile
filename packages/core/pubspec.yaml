name: core
description: Core package
version: 0.1.0+1
resolution: workspace
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  intl:
  equatable:
  json_annotation:
  meta:

dev_dependencies:
  test:
