name: auth
description: Auth data layer — models, sources, repository
version: 0.1.0+1
resolution: workspace
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  core:
  equatable:
  json_annotation:
  meta:
  storage_client:

dev_dependencies:
  build_runner:
  json_serializable:

flutter:
  uses-material-design: true
