name: facility
description: Facility data layer — venues, tables, sessions models, sources, repositories
version: 0.1.0+1
resolution: workspace
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  core:
  equatable:
  json_annotation:
  meta:
  api_client:

dev_dependencies:
  build_runner:
  json_serializable:
  test:

flutter:
  uses-material-design: true
